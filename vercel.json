{
  "framework": "vite",
  "installCommand": "corepack enable && corepack prepare yarn@4.14.1 --activate && git config --global url.\"https://x-access-token:${PRIVATE_REPO_TOKEN}@github.com/\".insteadOf \"https://github.com/\" && yarn install --no-immutable",
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
